services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"

  banking-service:
    build: .
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017/bankdb?retryWrites=false
    depends_on:
      - mongodb